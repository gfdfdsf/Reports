apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proyecto-integrador-devops.fullname" . }}-test-connection"
  labels:
    {{- include "proyecto-integrador-devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proyecto-integrador-devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
