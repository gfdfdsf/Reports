apiVersion: v1
kind: Service
metadata:
  name: {{ include "proyecto-integrador-devops.fullname" . }}
  labels:
    {{- include "proyecto-integrador-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proyecto-integrador-devops.selectorLabels" . | nindent 4 }}
